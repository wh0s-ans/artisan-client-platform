run = "cd backend && pip install -r requirements.txt && python init_db.py && uvicorn main:app --host 0.0.0.0 --port 8000"

[nix]
channel = "stable-24_05"

[deployment]
run = "cd backend && uvicorn main:app --host 0.0.0.0 --port 8000"

[[ports]]
localPort = 8000
externalPort = 80
